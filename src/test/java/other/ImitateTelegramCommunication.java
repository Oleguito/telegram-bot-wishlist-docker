package other;

import buttons.ButtonGenerator;
import handlers.WishlistTelegramBot;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.telegram.telegrambots.meta.api.objects.Chat;
import org.telegram.telegrambots.meta.api.objects.Message;
import org.telegram.telegrambots.meta.api.objects.Update;

public class ImitateTelegramCommunication {
    //
    // @Test
    // @DisplayName("When I click /start bot sends buttons")
    // void when_i_click_start_bot_sends_buttons() {
    //     WishlistTelegramBot bot = new WishlistTelegramBot();
    //     Update update = new Update();
    //     Message message = new Message();
    //     message.setChat(new Chat());
    //     message.setText("/start");
    //     update.setMessage(message);
    //     bot.onUpdateReceived(update);
    // }
    

}
